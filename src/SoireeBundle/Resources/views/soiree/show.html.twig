{% extends 'base.html.twig' %}

{% block body %}
    <h1>Soiree</h1>
    <div class="container-fluid">


        <table>
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soiree.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if soiree.date %}{{ soiree.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ soiree.theme }}</td>
            </tr>
            </tbody>
        </table>
        <div class="row">
            <div class="col-md-offset-3 col-md-8 color_back">
                <h1 class="titre_index">Soiree</h1>
                <div class="space"></div>
                <div class="row">
                    <div class="col-md-4">
                        <h2 class="text-center">Date</h2>
                        <div class="row liste_index text-center">
                            <td>{{ soiree.date|date('d-m-Y') }}<br /></td>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <h2 class="text-center">Thème</h2>
                        <div class="row liste_index text-center">
                            <td>{{ soiree.theme }}<br /></td>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <h2 class="text-center">Participants</h2>
                        <div class="row liste_index text-center">
                            <a href="{{ path('soiree_show', { 'id': soiree.id }) }}">Voir</a>
                        </div>
                    </div>
                </div>
                <div class="text-center">
                    <ul>
                        <li>
                            <a href="{{ path('soiree_index') }}">Back to the list</a>
                        </li>
                        <li>
                            <a href="{{ path('soiree_edit', { 'id': soiree.id }) }}">Edit</a>
                        </li>
                        <li>
                            {{ form_start(delete_form) }}
                            <input type="submit" value="Delete">
                            {{ form_end(delete_form) }}
                        </li>
                    </ul>
                </div>
            </div>
        </div>



        <ul>
            <li>
                <a href="{{ path('soiree_index') }}">Back to the list</a>
            </li>
            <li>
                <a href="{{ path('soiree_edit', { 'id': soiree.id }) }}">Edit</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </div>
{% endblock %}
